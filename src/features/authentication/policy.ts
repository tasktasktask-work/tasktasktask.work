/*
 * パスワードの決まりごと。
 *
 * ハッシュの実装から切り離してある。
 * @node-rs/argon2 は各プラットフォーム向けの実体を持つ拡張で、
 * ブラウザ側の束には入れられない。
 * 「8文字以上」という文言は入力欄にも出したいので、値だけをここへ置く。
 *
 * 文字種を強制しないのは、記号や数字を必須にすると
 * Password1! のような、規則を満たすだけの推測しやすい文字列に流れるためである。
 * 長さだけを条件にするほうが結果として強い。
 */

export const MIN_PASSWORD_LENGTH = 8;
